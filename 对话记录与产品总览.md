# LiteMD / MarkLite — 对话记录与产品总览

> 本文件是「整个对话记录 + 产物」的归集，对应 WorkBuddy 会话（2026-08-01，工作区 `F:\WorkBuddy\2026-08-01-00-05-56`）。
> 代码与文档产物已落地在桌面项目 **`C:\Users\manwa\Desktop\LiteMD`**。本工作区的原始记忆文件见 `LiteMD/memory/` 镜像。

---

## 0. 项目一句话定位

**LiteMD（产品名 MarkLite）** —— 一个超轻量化的 Markdown 编辑器，目标平台 **x86 Windows / x86 Linux / ARM Linux**，技术栈 **Rust + Tauri 2.x + Svelte + CodeMirror 6**。

---

## 1. 对话演进记录（按时间）

| # | 用户诉求 | 关键决策 / 方案 | 产出 |
|---|----------|----------------|------|
| 1 | 做一个超轻量化 md 编辑器，支持三平台，**先出设计方案** | 技术形式定为 **Rust + Tauri**（用户确认）；初版含写作/文档/发布三模式 | 设计草案（后废弃三模式） |
| 2 | ①三模式太复杂，**直接去掉**；②附上编辑器常用功能按键；③**左文件树 / 中编辑 / 右预览**，且文件树与预览要**可折叠** | 改为扁平工具栏 + 三栏布局；折叠通过侧栏头按钮控制 | 主界面浅色稿（Ardot Frame 2:2） |
| 3 | 左栏做成**二级文件夹**：一级大分类文件夹，二级是 `.md` 文件本身 | 文件树改为两层级结构 | 文件树样式更新 |
| 4 | **添加设置功能**（自动保存、本地文件目录、主题、更新等）+ **文件操作**（打开/保存/另存为/导出），其余我来设计 | 设置面板分标签页（通用/编辑器/外观/快捷键/导出/关于）；文件菜单含打开/保存/另存为/导出/设置/退出 | 设置·外观(2:265)、设置·通用(2:295)、文件菜单(2:391)、主界面深色(2:133) |
| 5 | **加上快捷键设置**（常用快捷键） | Ardot adapter 本次连接断开，无法写画布；改为交付**设计预览 + 规格文档** | `MarkLite-快捷键设置-spec.md` + Visualizer 预览 |
| 6 | 代码推荐用什么技术？ | **Rust + Tauri 2.x**（复用系统 WebView，不打包 Chromium，安装包仅数 MB，原生支持三端含 ARM64） | 技术选型结论（写入长期记忆） |
| 7 | 在 WorkBuddy 创建项目 **LiteMD**，文件放桌面 `LiteMD`，把内容移进去 | 确认范围：规格文档 + Rust+Tauri 脚手架；桌面属个人目录，已征求同意 | 桌面 `LiteMD` 完整脚手架 + 图标 + 移入 spec |
| 8 | 先出**项目脚手架方案**（目录结构 + Tauri 配置 + 依赖清单），再定 **CodeMirror vs Vditor** | 产出 `SCAFFOLD.md`；选型结论 **CodeMirror 6**（已落地集成） | `SCAFFOLD.md`、`EDITOR-SELECTION.md`、CodeMirror 6 接入代码 |
| 9 | 将整个对话记录和产物**移动到 LiteMD 空间** | 本文件（对话记录归集）；记忆镜像进 `LiteMD/memory/`；Ardot 画布待连接恢复后导出 | 本文 + 记忆镜像 |

---

## 2. 已确认的关键决策

**技术栈（用户已拍板）**
- 运行时：**Rust + Tauri 2.x**（不用 Electron —— 保证超轻量 + 三端覆盖，尤其 ARM64 Linux）
- 前端框架：**Svelte 4/5**（比 React 轻，编译期优化）
- 编辑器内核：**CodeMirror 6**（放弃 Vditor：其 IR/所见即所得把渲染混进编辑区，与「独立预览栏」三栏架构冲突，且包体约为 CM6 的 3 倍）
- 预览解析：**markdown-it + highlight.js**（`/lib/common` 仅常用语言，削减体积）
- 本地存储/配置：Tauri `fs` + TOML/JSON
- 打包：cargo 交叉编译出三端安装包

**设计原则（贯穿全程）**
- 三栏布局：左二级文件夹树 / 中编辑器（JetBrains Mono 源码）/ 右实时预览，**两侧可折叠**
- 双主题：浅（`#FFFFFF`/`#F7F8F9`，强调 `#0F6E56`）与深（`#1A1B1E`/`#25262B`）
- 字体：UI 用 Inter，编辑器源码用 JetBrains Mono
- 强调色（青绿）统一为 `#0F6E56`

---

## 3. 设计产物（Ardot 画布）

- 设计文件：**fileId `709917310604644`**，文件名「MarkLite 编辑器设计」
- 已建画面：
  - 主界面（浅色）Frame `2:2`
  - 主界面（深色）Frame `2:133`
  - 设置面板·外观 Frame `2:265`
  - 设置面板·通用 Frame `2:295`
  - 文件菜单下拉 Frame `2:391`
- 设置面板导航规划：通用 / 编辑器 / 外观 / **快捷键**（待补）/ 导出 / 关于
- **状态说明**：截至本会话，Ardot adapter 连接持续 `NO_ADAPTER`，「快捷键」标签页尚未写入画布，现有规格见 `MarkLite-快捷键设置-spec.md`。**待连接恢复后补入 Frame 2:265 / 2:295 系列。**

---

## 4. 代码产物清单（桌面 `LiteMD`）

```
LiteMD/
├── README.md              # 项目说明、运行/交叉编译命令
├── SCAFFOLD.md            # 脚手架方案：目录结构 / Tauri 配置 / 依赖清单 / 三端交叉编译矩阵
├── EDITOR-SELECTION.md    # 编辑器选型对比（CodeMirror 6 vs Vditor）与结论
├── MarkLite-快捷键设置-spec.md  # 快捷键设置页规格（分组快捷键 + 视觉规范）
├── 对话记录与产品总览.md   # 本文件
├── package.json           # 前端依赖（含 codemirror / @codemirror/* / markdown-it / highlight.js）
├── vite.config.ts / tsconfig.json / svelte.config.js / index.html
├── src/
│   ├── main.ts            # 前端入口
│   ├── App.svelte         # 三栏布局 + 工具栏 + 折叠 + 浅深主题
│   ├── editor.ts          # CodeMirror 6 封装：createEditor / setTheme / wrapSelection / toggleLinePrefix / insertLink
│   └── style.css          # 双主题 CSS 变量、工具栏、editor-host
├── src-tauri/
│   ├── Cargo.toml         # Rust 依赖（tauri / serde，release 开 opt-level="s"+lto+strip）
│   ├── build.rs / tauri.conf.json
│   ├── src/{main.rs, lib.rs}   # Tauri 命令占位（打开/保存/导出等）
│   ├── .gitignore / icons/     # 4 个青色 PNG 图标（Python 标准库生成）
├── memory/               # 工作区记忆文件镜像
└── dist/                # 前端构建产物（gitignored）
```

**验证状态**：`npm install`（101 包）+ `npm run build` 编译通过；gzip 主包约 300KB。

---

## 5. 文档产物清单

| 文档 | 内容 |
|------|------|
| `SCAFFOLD.md` | 脚手架方案：完整目录结构（带注释）、`tauri.conf.json` 关键字段、三端目标与交叉编译矩阵、前端 npm + Rust Cargo 依赖清单（版本+用途） |
| `EDITOR-SELECTION.md` | CodeMirror 6 vs Vditor 多维度对比表 + 结论 |
| `MarkLite-快捷键设置-spec.md` | 快捷键设置页规格：按 文件/编辑/格式/视图 分组、键位药丸样式、恢复默认交互 |
| 本文件 | 对话记录归集 + 产品总览 |

---

## 6. 待办 / 下一步

1. **Ardot 快捷键页补画布**：连接恢复后，按 `MarkLite-快捷键设置-spec.md` 把「快捷键」标签页补入设置面板。
2. **后端能力接入**（Rust `lib.rs`）：打开 / 保存 / 另存为 / 导出、自动保存、真实文件树读取（本地 `.md` 目录）。
3. **快捷键设置面板**：与 `MarkLite-快捷键设置-spec.md` 对齐，支持「点击重绑定」+「恢复默认」。
4. **超轻量优化**：`dist/` 当前含 100+ 语言 chunk（来自 `@codemirror/language-data` 全量 languages），建议裁剪为常用语言白名单，减小打包体积。
5. **双主题持久化**：把主题/设置项落到本地配置文件，随应用重启保持。

---

*生成时间：2026-08-01 · WorkBuddy 会话归集*
