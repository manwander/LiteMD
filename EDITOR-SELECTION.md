# 编辑器内核选型：CodeMirror 6 vs Vditor

> 结论先行：**采用 CodeMirror 6**。

---

## 1. 我们的产品约束（决策依据）

- **架构已定**：三栏布局 = 左文件树 / 中编辑器 / 右实时预览，中间编辑区是**独立源码视图**，右侧是**独立预览面板**，并有「编辑 / 分屏 / 预览」视图切换（见 Ardot 设计稿）。
- **核心诉求**：超轻量、三端覆盖（含 ARM Linux）、启动快、包体小。
- **已设计能力**：独立「快捷键设置」面板（文件/编辑/格式/视图分组，可重绑定）。

---

## 2. 候选对比

| 维度 | CodeMirror 6 | Vditor |
|------|--------------|--------|
| 定位 | 纯编辑器内核（源码式） | 一体化 Markdown 编辑器（3 种模式） |
| 包体（gzip 估算） | ~120–180KB（按需引入） | ~500KB+（含 IR/WYSIWYG 引擎与主题） |
| 编辑+预览关系 | 编辑与预览**解耦**：编辑器只管源码，预览由 markdown-it 另起面板 | 自带「即时渲染(IR)」「所见即所得」模式，**编辑与渲染合一** |
| 与我们三栏架构契合度 | ✅ 天然契合（编辑器 = 中栏，预览 = 右栏，各自独立） | ⚠️ 强耦合：IR 模式把渲染混进编辑区，与「独立预览栏」冲突 |
| 快捷键系统 | ✅ 一等公民 `keymap`，可重绑定、可读取，完美对接快捷键设置面板 | ⚠️ 内置固定快捷键，重绑定需 hack，难对接设置面板 |
| 自定义/主题 | ✅ 完全可控（Compartment 热切换浅/深主题） | ⚠️ 主题体系封闭，调成我们设计的青绿双主题成本高 |
| ARM Linux WebView 性能 | ✅ 轻 DOM、虚拟滚动行渲染，性能优 | ⚠️ IR 模式 DOM 较重 |
| 生态/维护 | ✅ 活跃、模块化、文档完备 | ◐ 国产、功能全但定制受限 |
| 上手成本 | 中（需自行拼装工具栏/预览） | 低（开箱即用，但难改） |

---

## 3. 为什么不是 Vditor

Vditor 的最大卖点是**零成本即得「所见即所得 / 即时渲染」的写作体验**。但这恰恰与我们已确定的产品形态冲突：

1. 我们的设计是**明确的「源码编辑区 + 独立预览区」双栏**，Vditor 的 IR 模式会把预览塞进编辑框，破坏既定架构与「分屏/预览」切换逻辑。
2. 「超轻量」是硬指标，Vditor 整体包体约为 CM6 的 3 倍以上。
3. 我们已专门设计了**可重绑定的快捷键面板**——CM6 的 `keymap` 是声明式、可编程的，能直接把设置面板里的快捷键映射到编辑器；Vditor 的快捷键是内置且封闭的。

> 若未来产品转向「沉浸式写作、弱化源码视图」，再评估 Vditor 的 IR 模式不迟。当前阶段 CM6 是更干净、更轻、更可控的选择。

---

## 4. 最终方案

- **编辑器内核**：CodeMirror 6（`basicSetup` + `@codemirror/lang-markdown` + 代码块多语言高亮）。
- **预览**：markdown-it + highlight.js，独立右栏渲染，与编辑器双向解耦。
- **主题**：用 `Compartment` 实现浅/深主题热切换（`@codemirror/theme-one-dark` 用于深色；浅色用自定义 `EditorView.theme` 对齐设计变量）。
- **工具栏/快捷键**：加粗、斜体、下划线、删除线、标题、列表、引用、链接等，封装为 `src/editor.ts` 中的命令函数，供工具栏按钮与快捷键面板统一调用。
- **轻量取舍**：默认不引入 `@codemirror/language-data` 亦可运行；开启后仅对代码块按需懒加载，不影响首屏体积。

详见 `src/editor.ts` 与 `src/App.svelte` 的实现。
