# LiteMD v1.0.1

> 首个正式版本 · 2026-08-07（v1.0.0 发布前补丁：修复双击打开 / 模式切换 / 全选问题）

超轻量 Markdown 编辑器，专注**大文档流畅编辑**与**低资源占用**。基于 Rust + Tauri 2 + Svelte 4 + CodeMirror 6。**安装包已内置 WebView2 运行时，双击即装，全程无需联网、无需任何额外步骤。**

---

## ✨ 核心特性

### 编辑体验
- 三栏布局：文件树 / 源码编辑器 / 实时预览，两侧可折叠
- 多标签编辑，会话级恢复（未保存内容、光标位置也会恢复）
- 预览编辑模式：直接在渲染后的预览上编辑，自动回写 Markdown
- 智能换行：`Enter` 自动跳出表格 / 代码块 / 引用；`Shift+Enter` 软换行
- 表格增强：一键插入模板、复制行、追加列、列对齐
- 有序列表自动重编号、格式刷、行内 ⚡ 快捷菜单、字体色 / 背景色标记

### 大文档（核心卖点，实测 20MB+）
- 分片流式载入：>8MB 文件头片先出字，剩余 Rust Channel 分片推送
- 增量切块预览：打字只重切脏区间（1MB 增量中位数 1.85ms）
- 虚拟化预览 + 高度记忆，滚动不掉帧
- 超大文档自动降级 + 低端设备检测（集显 / ≤4GB / ≤4核）

### 文件与图片
- 自动保存（防抖 800ms，可配 300~3000ms）
- 导出 HTML / PDF（Rust 侧渲染，A4 自动分页，中文字体）
- 粘贴图片自动收编：Worker 解码 / WebP 编码，哈希去重
- 孤儿附件清理（正则精确匹配，宁可漏删不误删）
- 跨文件查找 / 替换（批量替换带 `.bak` 原子回滚）

### 自定义
- 30 个快捷键全可重绑定，冲突检测、单条 / 全部恢复默认
- 浅色 / 深色主题，字号 11~24px 可调，专注模式（F11）

---

## 🐛 本版本修复

| 问题 | 状态 |
|------|------|
| 预览编辑模式：插入图片/表格后视口跳顶 | ✅ 已修复 |
| 预览编辑模式：最后一行输入时光标/滚动条不跟随 | ✅ 已修复 |
| 预览编辑模式：光标始终保持在倒数第二行，最后一行留白 | ✅ 已实现 |
| 源码编辑器：插入图片/表格/代码块后视口跳顶 | ✅ 已修复 |
| 清理未引用附件弹窗引用未定义变量构建失败 | ✅ 已修复 |
| **双击 .md 文件打开后停在首页，无法直接打开文档** | ✅ 已修复（Rust 端归一化文件路径：去引号 / 去 `file://` 前缀） |
| **预览编辑模式下全选会跳出预览编辑模式** | ✅ 已修复（非折叠选区不触发跟焦滚动；全局快捷键在预览编辑模式下不再误拦截） |
| **编辑 / 预览编辑 / 开关预览 切换有时不成功** | ✅ 已修复（进入预览编辑加 re-entrancy 守卫与失败回滚，避免半成品空白态） |

---

## 📦 安装

**Windows x64** 用户，下载下方任一安装包，**双击运行即可，无需任何额外安装**：

| 文件 | 说明 | 大小 |
|------|------|------|
| `LiteMD_1.0.1_x64-setup.exe` | NSIS 安装程序（推荐） | 约 206 MB |
| `LiteMD_1.0.1_x64_en-US.msi` | MSI 安装包 | 约 204 MB |

系统要求：Windows 10 / 11 x64。安装包已**内置 WebView2 运行时**，无需用户单独下载或安装，离线也能完成安装。

---

## 🔧 技术栈

Rust · Tauri 2.x · Svelte 4 · Vite 5 · CodeMirror 6 · markdown-it · highlight.js

应用本身轻量（无 Electron 臃肿依赖、内存/CPU 占用低）；为免去用户单独安装 WebView2 的麻烦，安装包已将 WebView2 运行时一并打包，故体积稍大但**开箱即用、离线可装**。

---

## 📄 详细文档

见仓库：`README.md` · `PERF.md` · `EDITOR-SELECTION.md`
